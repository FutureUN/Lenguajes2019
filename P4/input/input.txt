resource mult()
  const N := 20
  var a[N,N], b[N,N], c[N,N]: real

  proc multiply()
      var inner_prod := 0.0
      fa k := 1 to N ->
          inner_prod +:= a[i,k]
      af
      c[i,j] := inner_prod
  end
  final

  end
end